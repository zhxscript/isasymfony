nelmio_api_doc:
    documentation:
        info:
            title: ISA Symfony
            description: Basic Crud API for vehicles!
            version: 1.0.0
    areas: # to filter documented areas
        path_patterns:
            - ^/api/v1/(?!/doc$) # Accepts routes under /api except /api/doc
